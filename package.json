{
  "name": "ai-gateway",
  "version": "1.2.0",
  "private": true,
  "description": "Personal AI gateway - one OpenAI-compatible endpoint (streaming + tools) with multi-provider fallback across free tiers (Gemini, Groq, OpenRouter, Cerebras), a built-in chat UI, SQLite usage storage. Ready for Hermes Agent and any OpenAI SDK.",
  "main": "api/v1/chat.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "test": "node test/smoke.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "@upstash/redis": "^1.34.0"
  }
}
